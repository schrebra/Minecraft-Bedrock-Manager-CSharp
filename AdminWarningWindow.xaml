<Window x:Class="BedrockServerManager.AdminWarningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Administrator Privileges Recommended" Height="380" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" Background="#24273A"
        FontFamily="pack://application:,,,/Fonts/#NOTOSANSMONO-REGULAR"
        Icon="pack://application:,,,/minecraft.ico">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="AccentGreen" Color="#A6DA95"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#8AADF4"/>
        <SolidColorBrush x:Key="AccentRed" Color="#ED8796"/>
        <SolidColorBrush x:Key="AccentGray" Color="#5B6078"/>
        <SolidColorBrush x:Key="BgCard" Color="#363A4F"/>
        <SolidColorBrush x:Key="BorderDefault" Color="#5B6078"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#CAD3F5"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#B8C0E0"/>
        <SolidColorBrush x:Key="TextMuted" Color="#A5ADCB"/>

        <Style x:Key="TitleBarCloseButton" TargetType="Button">
            <Setter Property="Background" Value="#181926"/>
            <Setter Property="Foreground" Value="#CAD3F5"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#ED8796"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="FontFamily" Value="pack://application:,,,/Fonts/#NOTOSANS-REGULAR"/>
            <Setter Property="Foreground" Value="#24273A"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.70"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border BorderBrush="{StaticResource BorderDefault}" BorderThickness="1" CornerRadius="5" Margin="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Background="#181926">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="⚠️ Administrator Privileges Recommended" Foreground="#CAD3F5" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12" FontWeight="Bold"/>
                <Button Grid.Column="1" Name="btnClose" Style="{StaticResource TitleBarCloseButton}" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Data="M 0.5,0.5 L 9.5,9.5 M 0.5,9.5 L 9.5,0.5" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2" SnapsToDevicePixels="True"/>
                </Button>
            </Grid>

            <!-- Content Area -->
            <Grid Grid.Row="1" Margin="20,20,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Run as Administrator" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="For the best experience, run this application as an Administrator. This allows the Bedrock Server Manager to:" FontSize="12" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                        <TextBlock Text="🛡️" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="Automatically create and manage Windows Firewall rules for the server executable." FontSize="12" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" VerticalAlignment="Center" Width="400"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                        <TextBlock Text="🌐" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="Check and optimize network adapter settings (like setting a static IP) for a stable multiplayer environment." FontSize="12" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" VerticalAlignment="Center" Width="400"/>
                    </StackPanel>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <CheckBox x:Name="chkDontShowAgain" Content="Don't show me again" Foreground="{StaticResource TextSecondary}" Margin="0,0,15,0" VerticalAlignment="Center" FontSize="11"/>
                    <Button x:Name="btnContinue" Content="Continue Anyway" Background="{StaticResource AccentGray}" Foreground="#CAD3F5" Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
                    <Button x:Name="btnRelaunch" Content="🚀 Relaunch as Admin" Background="{StaticResource AccentGreen}" Style="{StaticResource ActionButton}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>